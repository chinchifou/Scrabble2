PRIMARY

check TODO in code

remove all pos_x from Sprite

create "update" for buttons to update state base on mouse cursor position

/!\ warning for "PLAY_LETTER", the letter must not always go where the player release the click. More calculation to do use collidelistall()

create draw all() function ? see for background copy and buttons and scores ...
def draw all()
	layer_background.draw()
	layer_letters.draw()
	...

create a class Action()

create function for log ?

auto shuffling for letters in hand



BUG

fix a glitch on LAPTOP (small black strip) for board when big window (bug already present in Scrabble V1) need to fix image

fix a glitch on LAPTOP when starting windowed but at max reoution (old sprite appearing the right size after resizing to biggest window size)
> try to force the window to pop IN the screen in the first time

bug when moving a letter above the board where letters are already present

bug : able to to click on end turn even if letter in hand



SECONDARY

possibility to save different games rules (board layout, letters in bag)

less highlighted buttons (2-3 pixel less pronounced)

Able to change the language of the UI -> text file for ui text in different languages

use save game

allow to change display settings while game is running

play music if "SCRABBLE"

The event queue offers some simple filtering. This can help performance slightly by blocking certain event types from the queue, use the pygame.event.set_allowed() and pygame.event.set_blocked() to work with this filtering


TO SEE 

add a priority level for layer ?

tile size 2 times smaller ??
